#ifndef _MAIN_H_
#define _MAIN_H_

/* Use these when printing data, as that's how the user inputs them */
#define LENGTH_FACTOR	A /* lengths are in angstrom */
#define TIME_FACTOR	FEMTOSECONDS  /* time is in femtoseconds */

void die(const char *fmt, ...);

#endif
