gamma = '5e11';
dt = 10;
sweeps = 20e3;
dropLogFactor = 3;
DLdataStartIndex = 2;

loadFromCache = false;

T = 10;
plotEndToEndZipping(5, T, 10, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(6, T, 10, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(7, T, 10, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(8, T, 10, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(10, T, 20, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(12, T, 20, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(14, T, 20, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(17, T, 25, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(20, T, 30, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(24, T, 40, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(28, T, 45, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(34, T, 60, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(40, T, 70, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(48, T, 100, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
%plotEndToEndZipping(57, T, 120, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
%plotEndToEndZipping(67, T, 160, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
%plotEndToEndZipping(80, T, 200, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);



T = 30;
plotEndToEndZipping(5, T, 15, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(6, T, 15, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);   
plotEndToEndZipping(7, T, 15, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(8, T, 15, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);   
plotEndToEndZipping(10, T, 35, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(12, T, 35, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);  
plotEndToEndZipping(14, T, 35, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(17, T, 40, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);  
plotEndToEndZipping(20, T, 50, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(24, T, 60, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);  
plotEndToEndZipping(28, T, 75, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(34, T, 100, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);  
plotEndToEndZipping(40, T, 120, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
plotEndToEndZipping(48, T, 160, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache); 
%plotEndToEndZipping(57, T, 200, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
%plotEndToEndZipping(67, T, 280, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache); 
%plotEndToEndZipping(80, T, 350, sweeps, '*', gamma, dt, dropLogFactor, DLdataStartIndex, loadFromCache);
