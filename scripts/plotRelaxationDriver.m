% WARNING, THIS OVERWRITES THE CACHED FILE!
% WARNING, THIS OVERWRITES THE CACHED FILE!
% WARNING, THIS OVERWRITES THE CACHED FILE!
% WARNING, THIS OVERWRITES THE CACHED FILE!
% WARNING, THIS OVERWRITES THE CACHED FILE!

gamma = '5e11';
dt = 10;
sweeps = 20e3;

dropLogFactor = 10;
DLdataStartIndex = 2;

T = 10;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 5,   10,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 6,   10,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 7,   10,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 8,   10,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 10,  20,  10, 20 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 12,  20,  10, 20 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 14,  20,  10, 20 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 17,  25,  12, 24 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 20,  30,  15, 30 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 24,  40,  25, 50 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 28,  45,  30, 60 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 34,  60,  45, 90 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 40,  70,  60, 120); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 48, 100,  85, 170); pause;
%%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 57, 120, 120, 240); pause;
%%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 67, 160, 170, 340); pause;
%%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 80, 200, 250, 500); pause;


T = 30;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 5,   15,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 6,   15,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 7,   15,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 8,   15,   5, 10 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 10,  35,  10, 20 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 12,  35,  10, 20 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 14,  35,  10, 20 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 17,  40,  12, 24 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 20,  50,  15, 30 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 24,  60,  25, 50 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 28,  75,  30, 60 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 34, 100,  45, 90 ); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 40, 120,  60, 120); pause;
plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 48, 160,  85, 170); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 57, 200, 120, 240); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 67, 280, 250, 500); pause;
%plotRelaxation(T, gamma, dt, sweeps, dropLogFactor, DLdataStartIndex, 80, 350, 250, 500); pause;
